
const SearchBar = (
  state = {input: ""}
  const searchElemnt = (value) => ({
    setState({input.key.value})
  })
  return (
    <div>
    <input class="input" type="search" onclick={searchElemnt} value="inuput" />
    <button class="savedBtn">Save</button>
  </div>
  )
)

export default SearchBar
